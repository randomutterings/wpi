="<div id='article#{news_counter + 1}' style='background: url(#{news.background_image.url}) no-repeat right;height:146px;' class='#{"hidden " if news_counter > 0}article'>"
#cycle_article
  #news_title
    =news.title
  =news.content.slice(0..280)
.padding_left.gold_links
  = link_to "Read more", news
  &nbsp;
  - unless @articles.length < 2
    = link_to_function "Next", "newsCycle.next(#{news_counter + 1})"
="</div>"