- title "Statuses"

%table
  %tr
    %th Name
  - for status in @statuses
    %tr
      %td= h status.name
      %td= link_to 'Show', status
      %td= link_to 'Edit', edit_status_path(status)
      %td= link_to 'Destroy', status, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Status", new_status_path
