%div.photo.clear
  - unless f.object.new_record?
    %div.float_left.padding_right.padding_bottom= image_tag(f.object.photo.url(:small))
  = f.file_field :photo
  = remove_link_unless_new_record(f)
  %div
    = f.check_box :primary, :class => "primary_photo_check_box"
    Make primary